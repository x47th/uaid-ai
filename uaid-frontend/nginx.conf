server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
    location /api/graphrag/ { proxy_pass http://graphrag-api:3001; }
    location /api/crm/ { proxy_pass http://horizon-api:3000; }
}